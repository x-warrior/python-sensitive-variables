language: generic
dist: bionic

before_install:
  - sudo apt-get update
  - sudo apt install -y python3-pip

install:
  - pip3 install setuptools
  - pip3 install --user nox

script:
  - nox -e test
  - nox -e lint
